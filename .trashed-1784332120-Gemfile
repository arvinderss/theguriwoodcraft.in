# GitHub Pages builds this automatically — you do NOT need to run anything
# locally. This file just declares the plugins so a local preview (optional)
# matches what GitHub builds.
source "https://rubygems.org"
gem "github-pages", group: :jekyll_plugins
gem "jekyll-sitemap"
gem "jekyll-seo-tag"
